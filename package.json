{
  "name": "mif-links",
  "version": "0.0.2",
  "scripts": {
    "build": "cd js && node_modules/.bin/gulp",
    "postinstall": "cd js && npm install && cd ../doc && npm install"
  },
  "dependencies": {
    "angular-datepicker": "^1.0.5"
  }
}
