{
  "name": "mif-links",
  "version": "0.0.1",
  "scripts": {
    "build": "cd js && NODE_ENV=production node_modules/.bin/gulp",
    "build-dev": "cd js && NODE_ENV=development node_modules/.bin/gulp",
    "postinstall": "cd js && npm install"
  }
}
